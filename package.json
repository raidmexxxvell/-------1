{
  "name": "liga-obninsk",
  "version": "1.0.0",
  "description": "Football league management system",
  "main": "app.py",
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "dev": "npx tsc -p tsconfig.json --watch"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
