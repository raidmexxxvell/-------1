# Roadmap развития проекта "Лига Обнинска" (Адаптированный)

## 🎯 Текущее состояние проекта

Проект представляет собой полнофункциональное веб-приложение для футбольной лиги с системой ставок, интегрированное с Telegram WebApp. Развернуто на Render.com и готово к использованию для аудитории до 5000 пользователей.

**Целевая аудитория**: 5000 активных пользователей максимум  
**Фокус**: Простота, вовлеченность, производительность  
**Ограничения**: Минимизация нагрузки на сервер и БД  
**Принцип**: Каждая функция должна приносить ценность без сложности  

---

## 📋 Фаза 1: Основные функции (✅ ГОТОВО)

### ✅ Инфраструктура и архитектура
- [x] Flask backend с модульной архитектурой API
- [x] PostgreSQL база данных с SQLAlchemy ORM
- [x] Redis кэширование для производительности
- [x] Многоуровневая система кэширования (Memory → Redis → Database)
- [x] WebSocket поддержка для real-time обновлений
- [x] Деплой на Render.com с автоматическим CI/CD

### ✅ Система безопасности
- [x] Аутентификация через Telegram WebApp
- [x] Rate limiting для защиты от спама (20 req/sec, bucket capacity 20)
- [x] Валидация входных данных
- [x] CSRF защита
- [x] Middleware для обработки ошибок и мониторинга

### ✅ Система ставок
- [x] Размещение ставок на исходы матчей (1x2)
- [x] Ставки на тоталы (больше/меньше голов)
- [x] Специальные ставки (пенальти, красные карточки)
- [x] Динамическое формирование коэффициентов
- [x] Автоматический расчет выигрышей
- [x] История ставок пользователя
- [x] Лимиты на ставки (мин/макс, дневные лимиты)

### ✅ Пользовательский интерфейс
- [x] SPA на Vanilla JavaScript с модульной архитектурой
- [x] Адаптивный дизайн для мобильных устройств
- [x] Система темизации (UFO League, BLB League)
- [x] Интеграция с Telegram WebApp API
- [x] Throttling UI событий для производительности

### ✅ Функции лиги
- [x] Турнирная таблица с live обновлениями
- [x] Расписание матчей
- [x] Результаты матчей
- [x] Статистика игроков и команд
- [x] Live статус матчей
- [x] Трансляции матчей в приложении

### ✅ Геймификация
- [x] Система достижений (9 категорий)
- [x] Ежедневный чек-ин с наградами
- [x] Система уровней и опыта
- [x] Кредиты как внутренняя валюта
- [x] Реферальная система
- [x] Лидерборды (прогнозисты, богатство, сервер)
  - [x] Описания достижений и UI "Подробнее" — добавлено в клиент: длинные и короткие тексты для основных групп достижений (bigodds, markets, weeks) и кнопка раскрытия в карточке достижения

---

## 🚀 Фаза 2: Оптимизация и улучшения (3-6 месяцев)

### ⚡ Стратегия мгновенной синхронизации данных (новое)
Внедряем набор паттернов для одновременного ускорения отклика и снижения нагрузки:
1. Stale-While-Revalidate + ETag (универсально)
  - [x] Новости (/api/news)
  - [x] Лидерборды (все 4 эндпоинта через etag_json helper)
  - [x] Достижения (/api/achievements)
  - [x] Статус чек-ина (GET /api/checkin)
  - [~] Match details (/api/match-details) — клиент частично унифицирован (`league.js` на `fetchMatchDetails`, остальные legacy вызовы в процессе), серверный `etag_json` helper ещё не применён
  - [x] Список результатов (/api/results) — клиент переведён на fetchEtag (fallback сохранён), серверный helper планируется
2. Универсальный helper etag_json(endpoint_key, payload_builder)
  - [x] Реализован в app.py
  - [x] Применён к /api/checkin (GET)
  - [x] Применён к leaderboards (4 эндпоинта) — устранено дублирование и унифицированы заголовки
  - [ ] Применить к /api/schedule (клиент уже на fetchEtag; сервер — план)
  - [ ] Применить к /api/results (клиент уже на fetchEtag; сервер — план)
  - [ ] Применить к /api/match-details (после нормализации core payload; клиент частично готов)
  - [ ] Вынести core_filter паттерн в документацию (используется для исключения updated_at из ETag)
3. Точечные push-обновления (WebSocket + версии полей)
  - [ ] Внедрить lightweight diff для score/markets (payload: {type:'patch', entity:'match', id, fields:{score:"1:0"}, version})
  - [ ] Версионность коэффициентов (odds_version) → клиент сверяет перед обновлением UI
4. Фоновые «снимки» (precompute)
  - [ ] Периодический прогрев leaderboards (каждые 60s) в Redis json
  - [ ] Предрасчёт weekly ROI для будущего достижения "Аналитик недели"
  - [ ] Precompute aggregated betting stats на пользователя (используется в achievements)
5. Событийная инвалидация через Redis pub/sub
  - [ ] Публикация событий: match:update, news:mutate, bets:new
  - [ ] Локальная подписка smart_invalidator → выборочное очищение ключей
6. Пакетирование частых изменений
  - [ ] Server-side debounce 250ms для live событий перед broadcast
  - [ ] Агрегация нескольких мелких изменений матча в один WebSocket пакет
7. Клиентский SWR слой
  - [x] achievements localStorage + If-None-Match
  - [x] checkin status ETag
  - [x] Унифицированная утилита fetchEtag(url, cacheKey)
    - [x] Базовая утилита `etag-fetch.js`
    - [x] /api/achievements мигрирован
    - [x] /api/schedule мигрирован (loadSchedule + безопасный fallback)
    - [x] /api/results мигрирован (loadResults + безопасный fallback)
    - [~] /api/match-details частично (новые вызовы через `fetchMatchDetails`; часть старых прямых fetch ещё остаются)
    - [ ] Leaderboards фронтенд: заменить локальные etagFetch на fetchEtag (сохранить ключи версий)
  - [x] Добавлена специализированная обёртка `match-details-fetch.js` (roster-aware cache write)

9. Splash экран и UX прогресса (новое)
  - [x] Числовой индикатор прогресса 0–100% под полосой
  - [x] Stage API: `splashStages.profile()` (70%), `splashStages.data()` (90%), `splashStages.finish()` (100%)
  - [ ] Интеграция stage вызовов в реальные точки завершения загрузки (achievements, schedule, results, lineups) вместо тайминговых эвристик
  - [ ] Убрать лишние интервальные обновления после внедрения реальных хуков

10. Надёжность БД (транзиентные ошибки)
  - [x] Пилотный retry + engine.dispose для `/api/betting/my-bets` (OperationalError / SSL EOF)
  - [ ] Централизованный helper (экспоненциальный backoff, метрики попыток)
  - [ ] Применить к критическим чтениям leaderboards / achievements / lineups

UI / UX мелкие правки (быстрый финиш)
  - [x] Стилизованная модалка подтверждения в стиле приложения (замена native confirm)
  - [x] Центрирование заголовка «Новости» на главной
  - [x] Компактная/адаптивная таблица заказов в админке (сокращённые заголовки, responsive hide)
  - [x] Расширенные описания достижений + кнопка «Подробнее» (исключены placeholder'ы)
8. Метрики эффективности (после внедрения)
  - [ ] Логировать процент ответов 304 / суммарно
  - [ ] Среднее время генерации payload vs отдача из кэша
  - [ ] Кол-во WebSocket сообщений/мин после batching


### ⚽ Управление командами и матчами

#### 👥 Система заполнения составов капитанами
- [x] **Заполнение составов капитанами команд** (актуализировано: real-time `lineups_updated`, toast-уведомления, inline валидация дублей, удалена нумерация, persistent roster + fallback)
  - [x] Возможность капитанам заполнять составы за 2 часа до матча
    1. ✅ Добавить роль "Капитан команды" в User модель (database_models.py)
    2. ✅ Создать простой HTML интерфейс для выбора состава
    3. ✅ Реализовать временные ограничения через JavaScript таймер
  - [x] Система подтверждения составов через админа
    1. ✅ Добавить таблицу LineupSubmission в БД (match_id, team_id, players_json, status)
    2. ✅ Расширить существующую админ-панель простым списком составов
    3. ✅ Уведомления капитанам через существующую систему Telegram WebApp
  - [ ] Автоматические напоминания
    1. Добавить задачу в background_tasks.py для проверки времени до матча
    2. Отправлять push через Telegram WebApp API
    3. Простая блокировка формы после дедлайна

##### Persistent Roster (добавлено)
- [x] Таблица `team_roster` (ad-hoc creation) для хранения последних подтверждённых основных составов
- [x] Дедупликация игроков (case-insensitive нормализация, trim + collapse spaces)
- [x] Авто-синхронизация: при сохранении матчевого состава обновляются записи roster (добавление новых / удаление убранных)
- [x] Fallback: если у матча нет сохранённых составов — публичный и админ интерфейсы используют `team_roster`
- [x] Новый публичный эндпоинт `GET /api/match/lineups?match_id=...` (DB-first, без Google Sheets)
- [ ] Миграция Alembic для `team_roster` (устранение inline DDL)
- [ ] Унификация нормализации (вынести helper)

#### 📰 Блок новостей на главной
- [x] **Компактная новостная лента (не более 5 записей)**
  - [x] Простой блок новостей
    1. ✅ Создать модель News в database_models.py (id, title, content, date, author_id)
    2. ✅ Добавить CRUD в существующую админ-панель
    3. ✅ Вывести на главную с кэшированием 5 минут через multilevel_cache.py
  4. ✅ Стилизация клиентского блока (карточки, адаптив, hover)
  5. ✅ Инвалидация кэша новостей при create/update/delete (invalidate_pattern)
  - [ ] Автоматические анонсы матчей
    1. Генерировать анонсы в background_tasks.py за 24 часа до матча
    2. Простой шаблон: "Завтра в {time} - {home} vs {away}"
    3. Автоматически добавлять в новостную ленту

#### 🛠 Исправления статистики матчей
- [x] Инкремент matches_played при завершении матча
  1. ✅ Добавить обновление PlayerStatistics.matches_played при статусе finished
  2. (Опционально) Пересчитать исторические данные через /api/statistics/refresh
  3. ✅ Кэш-инвалидация stats_table после обновления
  4. [ ] (План) Оптимизация: добавить поле match_id в MatchFlags + миграция
     - Создать миграцию (ALTER TABLE match_flags ADD COLUMN match_id INT NULL)
     - Заполнить match_id для существующих строк сопоставлением по home/away (и дате) с таблицей matches
     - Добавить индекс (CREATE INDEX IF NOT EXISTS ix_match_flags_match_id ON match_flags(match_id))
     - Обновить админ-эндпоинт завершения матча: использовать прямой match_id вместо поиска по названиям
     - Упростить пересчёт stats: один INSERT ... SELECT по match_id (без цикла по игрокам)
     - После внедрения удалить зависимость от строковых home/away в логике статистики (только для идентификации матча)

  #### 🎥 Улучшения и фикс системы трансляций
  - [x] Фикс утечки панели трансляции между вкладками матча
    1. ✅ Добавлено скрытие `#md-pane-stream` при переходе на любую вкладку кроме `stream`
    2. ✅ Обновлён `MatchStream.deactivate`: снимает `fs-mode`, `allow-landscape`, скрывает панель
    3. ✅ Обновлён обработчик саб-вкладок в `profile-match-advanced.js` (принудительное `display='none'` + вызов деактивации)
    4. ✅ Исключена ситуация «залипания» ориентации в landscape после ухода со вкладки
  - [ ] Lazy-unload iframe при скрытии (освобождение ресурсов, опциональный параметр сохранения состояния)
  - [ ] Адаптивная подгрузка: инициализировать поток только по явному заходу во вкладку (если трафик критичен)
  - [ ] Авто-перенос в картинку-в-картинке (PiP) при сворачивании (если поддерживается браузером)
  - [ ] A11y: переключать `aria-hidden` и `inert` для неактивных панелей
  - [ ] Watchdog перезапуска iframe при network stall (>30s без новых кадров) — лёгкая эвристика (timeout + reload src)
  - [ ] Кэш последнего успешного `vkVideoId` в sessionStorage для ускорения возврата
  - [ ] Опциональная кнопка «Спрятать поток» (экономия батареи / трафика) – выгружает iframe до повторного входа
  - [ ] Сбор простых метрик (время просмотра трансляции) для будущего анализа вовлеченности

  #### 📆 Сезонный rollover (сброс статистики по окончании сезона)
  - [x] Endpoint /api/admin/season/rollover
    1. ✅ Завершение активного турнира (status=completed, end_date = today)
    2. ✅ Генерация строки следующего сезона (формат YY-YY)
    3. ✅ Создание нового Tournament (status=active)
    4. ✅ Сброс legacy таблиц: team_player_stats, match_scores, match_player_events, match_lineups, match_stats, match_flags
    5. ✅ Инвалидация ключевых кэшей (league_table, stats_table, results, schedule, tours, betting-tours)
    6. [ ] Hook авто-анонса в новости (ещё не реализовано)
    7. ✅ Опция мягкого режима (?soft=1)
    8. ✅ Лог аудита rollover (season_rollovers) c pre/post hash & meta
    9. ✅ Dry-run (?dry=1)
    10. ✅ UI двойное подтверждение полного режима
    11. ✅ Rate-limit (10 минут)
    12. ✅ SHA-256 снимки pre/post
    13. ✅ Fallback вход /admin/login (cookie HMAC) + require_admin
    14. ✅ Deep режим (?deep=1) для полного: advanced очистка (matches, match_events, team_compositions, player_statistics)
    15. ✅ Импорт расписания из Google Sheets (до 300 строк) в deep
    16. ✅ Очистка столбцов B и D листа РАСПИСАНИЕ ИГР (B2:B300,D2:D300) перед импортом при deep
    17. ✅ UI чекбокс deep в админ-панели
    18. ✅ Фоновый прогрев кэша после rollover (отдельный поток)
  19. ✅ Прогрев news кэша (limit=5) после CRUD
  20. ✅ ETag + Cache-Control для /api/news (экономия трафика)

### 💰 Система подписок (4 уровня)

#### 💎 Многоуровневая подписка (простая монетизация)
- [ ] **Бесплатная (по умолчанию)**
  - [x] Базовые ставки 1x2, тоталы (уже есть)
  - [x] Основные достижения (уже есть)
  - [x] Стандартная статистика (уже есть)
  - [ ] Лимит: 5 ставок в день
    1. Добавить поле daily_bet_limit в User модель
    2. Проверка лимита в betting API
  
- [ ] **Базовая (100₽/месяц)**
  - [ ] Все типы ставок (включая специальные)
  - [ ] Расширенная статистика (графики ROI)
  - [ ] Приоритет в поддержке (badge в профиле)
  - [ ] Лимит: 15 ставок в день
    1. Добавить subscription_level enum в User модель
    2. Создать middleware для проверки лимитов
    3. Интеграция с ЮKassa для простых платежей

- [ ] **Средняя (200₽/месяц)**
  - [ ] Эксклюзивные коэффициенты (+5% к обычным)
  - [ ] Детальная аналитика прогнозов (win rate по рынкам)
  - [ ] VIP значок в профиле и лидербордах
  - [ ] Без лимитов на ставки
    1. Модификация расчета коэффициентов в utils/betting.py
    2. Добавить VIP стили в CSS
    3. Расширить статистику в profile.js

- [ ] **Премиум (400₽/месяц)**
  - [ ] Персональные рекомендации (простой алгоритм на основе истории)
  - [ ] Ранний доступ к линии (на 30 минут раньше)
  - [ ] Эксклюзивные достижения и титулы
  - [ ] Приоритет в лидербордах при равных показателях
    1. Простая рекомендательная система: анализ успешных паттернов пользователя
    2. Задержка публикации линии для не-премиум пользователей
    3. Специальные достижения только для premium

### 🎮 Легкая геймификация (без перегрузки)

#### 📊 Улучшение системы достижений
- [ ] **2-3 новые простые категории**
  - [ ] "Аналитик недели" - лучший ROI за неделю
    1. Еженедельный расчет в background_tasks.py
    2. Автоматическое начисление достижения
  - [ ] "Верный болельщик" - ставки на одну команду
    1. Трекинг favorite_team в существующей betting системе
    2. Достижения за 5, 10, 20 ставок на одну команду
  - [ ] "Марафонец" - непрерывная активность
    1. Подсчет streak_days в User модели
    2. Достижения за 7, 14, 30 дней подряд

#### 🏆 Простые улучшения лидербордов
- [ ] **Еженедельные мини-соревнования**
  - [ ] Топ прогнозистов недели (кэш на 1 час)
  - [ ] Топ по прибыли за неделю
  - [ ] Простые призы в кредитах (100-500)
    1. Автоматическое начисление в воскресенье через background_tasks.py
    2. Уведомления через Telegram WebApp
    3. Кэширование результатов для снижения нагрузки на БД

### 🛠️ Техническая оптимизация (критично для стабильности)

#### ⚡ Улучшение производительности
- [ ] **Постепенный рефакторинг app.py**
  - [ ] Этап 1: Вынести betting логику
    1. Создать services/betting_service.py
    2. Перенести функции размещения ставок
    3. Перенести расчет коэффициентов
  - [ ] Этап 2: Оптимизация БД
    1. Добавить недостающие индексы (bet.user_id, match.date)
    2. Оптимизировать N+1 запросы в лидербордах
    3. Использовать bulk operations для массовых обновлений
  
- [ ] **Улучшение кэширования (важно для 5000 пользователей)**
  - [ ] Увеличить время кэша для статичных данных
    1. Турнирная таблица: 10 минут (было 5)
    2. Лидерборды: 30 минут (было 60)
    3. Новости: 15 минут
  - [ ] Предзагрузка популярных данных
    1. Кэшировать главную страницу полностью
    2. Предзагружать матчи дня в фоне
  - [ ] Расширение паттерна etag_json на все GET, где payload <= ~50KB
  - [ ] Вынос громоздких блоков ETag логики из лидеров в helper (устранить дублирование)
  - [ ] Добавить центральный registry для invalidate_pattern → pub/sub
  - [ ] Централизованный retry/backoff helper для транзиентных ошибок БД (OperationalError, SSL EOF) — пилот реализован в /api/betting/my-bets
  - [ ] Alembic миграция: вынести inline DDL (team_roster) + генерация ревизии
  - [ ] Sanitization контента новостей через bleach (XSS защита)
  - [ ] Унификация нормализации имён игроков (single helper) — используется в lineups & roster sync
  - [ ] Стандартизировать ключи localStorage (namespace v1:, убрать *:etag-temp после полной миграции)
  - [ ] Добавить lightweight perf логгер вокруг etag_json (ms_build, cache_hit)

#### 🔄 Реализация push/diff обновлений
- [ ] Добавить версионное поле version/updated_at во все payload (стандартизировать)
- [ ] WebSocket событие data_patch (match, odds, news)
- [ ] Клиентский reconciler: применять только изменившиеся узлы DOM
 - [ ] Унифицированный формат diff: {type:'patch', entity, key, fields, v}
 - [ ] Бэтчинг событий матчей (score+odds) ≤1 пакет/250ms

#### 🧪 Мониторинг внедрённых оптимизаций
- [ ] Тайминги: логировать ms генерации vs ms кэш-ответа
- [ ] Счетчик cache hit/miss для etag_json
- [ ] Export простых метрик в /health/perf (JSON)

#### 📱 Минимальные UI улучшения
- [ ] **Простые улучшения UX**
  - [ ] Завершить темную тему (основа уже есть в CSS)
    1. Доработать темную схему для всех компонентов
    2. Добавить переключатель в профиль
    3. Сохранение в localStorage
  - [ ] Базовые анимации
    1. CSS transitions для кнопок (0.2s ease)
    2. Smooth scroll для списков
    3. Loading спиннеры для AJAX запросов

---

## 🌟 Фаза 3: Социальные функции (легкие) (6-9 месяцев)

### 👥 Базовые социальные функции

#### 💬 Простая система комментариев
- [ ] **Комментарии к матчам (без сложности)**
  - [ ] Простые комментарии под матчами
    1. Таблица Comments (match_id, user_id, text, created_at)
    2. Простая HTML форма без rich-editor
    3. Базовая модерация (фильтр мата, лимит 500 символов)
  - [ ] Лайки/дизлайки (без сложной системы)
    1. Простые счетчики likes/dislikes в Comments
    2. AJAX запросы без real-time обновлений
    3. Лимит 1 реакция на пользователя

#### 👨‍👩‍👧‍👦 Легкие командные функции
- [ ] **Простые команды прогнозистов (до 5 человек)**
  - [ ] Создание мини-команд
    1. Таблица PredictionTeams (name, captain_id, members_json)
    2. Простой интерфейс приглашения по username
    3. Командный рейтинг = среднее арифметическое участников
  - [ ] Еженедельные командные соревнования
    1. Подсчет очков команды = сумма очков участников
    2. Простая страница с топ-10 команд
    3. Символические призы в кредитах (200-1000)

### 🎲 Простые мини-игры

#### 🎯 Легкие дополнительные активности
- [ ] **Еженедельная викторина (5-10 вопросов)**
  - [ ] Простая викторина о футболе
    1. JSON файл с вопросами (обновляется вручную)
    2. Простая форма с radio buttons
    3. Призы за правильные ответы (50-200 кредитов)  

---

## ⚡ Фаза 4: Техническая стабильность (9-12 месяцев)

### 🔧 Завершающие оптимизации

#### 🛡️ Улучшенная безопасность (без SMS)
- [ ] **Базовые меры безопасности**
  - [ ] Регулярные обновления зависимостей (ежемесячно)
    1. Проверка уязвимостей через GitHub Dependabot
    2. Тестирование после обновлений
  - [ ] Улучшение логирования (без сложных систем)
    1. Логирование критичных ошибок в файл
    2. Простая ротация логов (logrotate)
    3. Еженедельный обзор логов на предмет аномалий

#### 📊 Простая аналитика
- [ ] **Базовые метрики для админа**
  - [ ] Простой dashboard
    1. Основные KPI: DAU, MAU, количество ставок, revenue
    2. Простые графики на Chart.js (без тяжелых библиотек)
    3. Еженедельные отчеты на email админу
  
  - [ ] Пользовательская аналитика (простая)
    1. Личная статистика: ROI, успешность по типам ставок
    2. Простые bar charts прогресса
    3. Базовые рекомендации: "Вы лучше угадываете тоталы"

#### ✅ Критически важное тестирование
- [ ] **Базовое покрытие тестами**
  - [ ] Unit тесты для системы ставок
    1. Тесты размещения ставок (betting API)
    2. Тесты расчета выигрышей
    3. Тесты валидации лимитов
  
  - [ ] Простые интеграционные тесты
    1. Smoke тесты критических endpoints
    2. Тест авторизации через Telegram
    3. Тест кэширования основных данных

---

## 📈 Реалистичные метрики для 5000 пользователей

### 👥 Целевые показатели
- **Активные пользователи**: 200-500 DAU, 1000-2000 MAU
- **Удержание**: D7 > 40%, D30 > 20%
- **Вовлеченность**: 15+ минут в день для активных
- **Конверсия в подписки**: 5-10% пользователей (250-500 человек)

### 💰 Реалистичная монетизация
- **Базовая подписка (100₽)**: 50-100 пользователей = 5,000-10,000₽
- **Средняя подписка (200₽)**: 20-40 пользователей = 4,000-8,000₽
- **Премиум подписка (400₽)**: 10-20 пользователей = 4,000-8,000₽
- **Месячная выручка**: 13,000-26,000₽ (реально достижимо)

### 🎯 Техническая производительность (критично)
- **Время загрузки**: < 2 секунд для всех страниц
- **Uptime**: > 99% (важно для retention)
- **Пиковая нагрузка**: < 70% использования ресурсов Render
- **Размер БД**: автоархивация старых данных (>6 месяцев)

---

## 🛠️ Приоритеты разработки (реалистичные)

### Первоочередные задачи (1-3 месяца):
1. **Завершить унификацию match-details + leaderboards (fetchEtag + server etag_json)**
2. **Интегрировать splashStages в реальные загрузки и убрать временные заглушки**
3. **Alembic миграции (team_roster, подготовка схемы под подписки)**
4. **Sanitize новостей (bleach) + security hardening**
5. **Вынос betting & news из app.py (первый шаг декомпозиции)**
6. **Централизованный DB retry/backoff helper**
7. **Базовый тестовый каркас (pytest + smoke)**

### Средний приоритет (3-6 месяцев):
1. **Система подписок (без биллинга) — флаги доступа и лимиты**
2. **Легкие социальные функции** - комментарии
3. **Новые достижения (стрик, аналитик недели)**
4. **Отложенный push/diff канал + batching**
5. **Расширенные метрики (cache, etag, ws)**

### Долгосрочные цели (6-12 месяцев):
1. **Техническая стабильность** - тестирование критичных частей
2. **Простая аналитика** - базовые метрики и insights
3. **Улучшенная безопасность** - защита без перегрузки

## ❌ Исключенные функции (избегаем перегрузки)

### Не планируется:
- ❌ Мобильные приложения (достаточно PWA)
- ❌ SMS подтверждения (дорого для маленькой аудитории)
- ❌ Сложный ИИ и ML (излишне для 5000 пользователей)
- ❌ Биометрическая аутентификация
- ❌ Криптовалютные платежи
- ❌ Telegram боты (дублирование функционала)
- ❌ Интеграция с внешними спортивными API (дорого)
- ❌ Сложная система чатов и мессенджера
- ❌ A/B тестирование (не критично для малой аудитории)
- ❌ CDN и edge caching (не нужно на Render для такой нагрузки)

---

**Принцип разработки**: 
> "Простота превыше всего. Каждая новая функция должна приносить измеримую ценность пользователям без существенной нагрузки на сервер, сложности разработки или поддержки."

Фокус на стабильности, простоте и устойчивой монетизации для комфортного роста до 5000 пользователей.
