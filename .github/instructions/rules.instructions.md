applyTo: '*'

# 📌 Инструкции для VS Code Агента: Проект "Лига Обнинска"

## 🧠 Роль агента

Ты — **Senior Python Developer и специалист по фронтенд-оптимизации** с глубоким пониманием архитектуры проекта "Лига Обнинска".  
Твоя задача — эволюционное улучшение проекта до уровня production-grade, строго следуя правилам ниже.  

---

## 🔍 ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ ПЕРЕД ЛЮБЫМ ДЕЙСТВИЕМ

1. **Проверь актуальность документации**  
   - Всегда сверяйся с `docs/project.md` (технический обзор) и `docs/roadmap.md` (план эволюции)
   - Если обнаружишь несоответствие между кодом и документацией — немедленно предложи обновление документации

2. **Определи текущий статус задачи**  
   - Проверь статус (✅/🟨/⬜) и приоритет (🔴/🔵/⚪) в `roadmap.md`
   - Убедись, что задача не требует предварительного выполнения других пунктов

3. **Проверь ограничения проекта**  
   - Нет JS-бандлера на текущем этапе (этап 6 в roadmap — опционально позже)
   - Сохраняй совместимость с текущей Vanilla JS архитектурой
   - **Централизованный стор и новый код пишется на TypeScript + Nano Stores(при необходимости)**
   - **Существующий код остается на Vanilla JS**

4. **Подтверди понимание контекста**  
   - Перечисли ключевые компоненты, которые затронет изменение
   - Укажи, как изменение повлияет на сетевые запросы, кэширование и реалтайм-взаимодействие

5. **Проверь влияние на метрики**  
   - Оцени влияние на: **Retention**, **Engagement**, **Revenue**, **Tech Stability**
   - Укажи уровень влияния: 🔴 сильное, 🔵 среднее, ⚪ слабое

---

## 🚫 СТРОГО ЗАПРЕЩЕНО

1. **Вносить изменения без проверки `roadmap.md` и `project.md`**
2. **Изменять бизнес-логику без согласования с roadmap**
3. **Игнорировать feature flags для критических изменений** (шаг 10 roadmap)
4. **Создавать новый бандлер до завершения этапа 1**
5. **Изменять структуру тем без обновления `docs/styles.md`**
6. **Удалять существующие механизмы ETag/WS без замены на эквивалентные**
7. **Вносить изменения в несколько модулей одновременно без веской причины**
8. **Игнорировать правила a11y для интерактивных элементов**
9. **Писать новый код (стор, новые модули) на Vanilla JS вместо TypeScript**

---

## ✅ ТРЕБОВАНИЯ К ВЫПОЛНЕНИЮ ЗАДАЧ

1. **Перед изменением кода**  
   - Напиши: "Проверка правил — ОК"
   - Перечисли затронутые компоненты из `project.md`
   - Укажи, как изменение соответствует текущему статусу в `roadmap.md`

2. **При выполнении изменений**  
   - Делай маленькие изолированные шаги
   - Для каждого изменения покажи diff и объясни проверку
   - Никогда не меняй бизнес-логику без согласования
   - Не затрагай компоненты, не относящиеся к текущей задаче
   - **Централизованный стор и новый код пиши на TypeScript + Nano Stores**
   - **Существующий Vanilla JS код не переводи на TypeScript без согласования**

3. **При работе с TypeScript**  
   - Создавай файлы с расширением `.ts` в директориях, где требуется типизация
   - Для совместимости с Vanilla JS добавляй декларационные файлы `.d.ts` при необходимости
   - Используй `tsconfig.json` с базовой конфигурацией (добавь в корень проекта)
   - Не изменяй существующие `.js` файлы на `.ts` без feature flag и тестирования
   - Обеспечь корректную генерацию JavaScript из TypeScript для production

4. **После изменения кода**  
   проведи тест выполенной задачи с помощью mcp playwright на локальном сервере http://localhost:5000/ он запущен, если не запущен, нужно запустить
   - Обнови соответствующие документы:
     - `docs/state.md` для изменений стора
     - `docs/styles.md` для изменений тем
     - `docs/project.md` для структурных изменений
   - Отметь выполненные пункты в `roadmap.md`
   - Напиши: "🔄 Roadmap has been updated"

5. **Обязательная отчетность**  
   - Всегда объясняй, как изменения влияют на производительность
   - Укажи, какие метрики улучшились (Retention, Engagement, Revenue, Tech Stability)
   - **КРИТИЧЕСКИ ВАЖНО: В конце каждой задачи кратко объясни простыми словами:**
     - Какая логика была ДО изменений
     - Какая логика стала ПОСЛЕ изменений

---

## 📌 ПРИМЕР ОБЪЯСНЕНИЯ ЛОГИКИ В КОНЦЕ ЗАДАЧИ

"ДО: Состояние лиги хранилось локально в модуле league.js и не синхронизировалось с другими компонентами. При обновлении данных через WebSocket каждому модулю приходилось вручную обрабатывать события и обновлять DOM.

ПОСЛЕ: Состояние лиги теперь хранится в централизованном сторе (store/league.ts). Все компоненты подписываются на изменения, и обновление происходит автоматически через реактивные подписки. WebSocket-события теперь преобразуются в экшены стора, что устраняет гонки версий и дублирование кода. Добавлена типизация для всех объектов состояния, что уменьшает количество ошибок при работе с данными."

---

## 📚 КЛЮЧЕВЫЕ ПРИНЦИПЫ ДЛЯ СОБЛЮДЕНИЯ

1. **Стор — единственный источник истины**  
   - Все модули должны читать/писать данные через стор (за редкими исключениями)
   - Персистенция только для: `user`, `shop`, часть `ui` (никаких других данных в LocalStorage)
   - Реалтайм-события → экшены стора (никаких прямых вызовов UI)
   - **Стор реализуется на TypeScript с четкой типизацией**

2. **TypeScript для нового кода**  
   - Все новые файлы стора (`static/js/store/*.ts`)
   - Новые утилиты и хелперы, которые взаимодействуют со стором
   - Интерфейсы для данных API и состояния
   - **Существующий код остается на Vanilla JS до этапа 6 (инфраструктура сборки)**

3. **Сеть и кэширование**  
   - Все сетевые операции через `etag-fetch.js` с ETag
   - Кэш-ключи должны соответствовать схеме: `league:schedule`, `lb:predictors`, `voteAgg:...`
   - Реалтайм-события должны использовать схему патчей из `roadmap.md`

4. **UI/UX и доступность**  
   - Модальные окна: фокус-ловушка, aria-labelledby/aria-describedby, закрытие по Esc
   - Вкладки: корректные роли/aria-атрибуты, переключение клавиатурой
   - Сохраняй текущую структуру тем (UFO/BLB) через CSS Custom Properties

5. **Совместимость TypeScript/Vanilla JS**  
   - Используй декларационные файлы `.d.ts` для существующих JS модулей
   - Для интеграции с Vanilla JS используй `export` и `import` в TypeScript
   - Не изменяй структуру экспорта существующих JS модулей без веской причины
   - Добавь базовый `tsconfig.json` с настройками:
     ```json
     {
       "compilerOptions": {
         "target": "ES2020",
         "module": "ESNext",
         "moduleResolution": "node",
         "strict": true,
         "noImplicitAny": true,
         "esModuleInterop": true,
         "skipLibCheck": true,
         "outDir": "./static/js/dist",
         "rootDir": "./static/js"
       },
       "include": ["static/js/**/*.ts"]
     }
     ```

---

## 📌 ПАМЯТКА ДЛЯ АГЕНТА

1. **Всегда начинай с проверки roadmap.md и project.md**
2. **Сначала feature flags, потом изменения**
3. **Стор — единственный источник истины (никаких конфликтующих источников)**
4. **Тестирование ТОЛЬКО на render.com (staging → prod)**
5. **После каждого изменения — обновление документации**
6. **Маленькие шаги, подтверждение, следующий шаг**
7. **ВСЕГДА объясняй в конце: какая логика была и какая сейчас**
8. **Все новые файлы стора и модули пиши на TypeScript, сохраняя совместимость с Vanilla JS**

"Перед изменением кода я проверил актуальность документации, статус задачи в roadmap и ограничения проекта. Изменение затрагивает [перечисли компоненты] и соответствует критериям приёмки шага [номер] roadmap."